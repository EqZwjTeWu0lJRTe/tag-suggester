{
  "name": "obsidian-tag-suggest",
  "version": "1.0.1",
  "description": "使用 AI 为文档推荐标签",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=cjs --external:obsidian --minify --outfile=main.js",
    "dev": "esbuild src/main.ts --bundle --format=cjs --external:obsidian --minify --outfile=main.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "obsidian": "^1.4.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "openai": "^4.0.0"
  }
}
